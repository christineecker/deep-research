---
name: stage-0-mechanics
description: Stage 0's five-part question set and setup commands, from Lesson 2
metadata:
  type: learning-record
---

## What this covers

Lesson 2 (`lessons/0002-stage-0-configure.html`) went one level past Lesson 1's shorthand into
the actual Stage 0 conversation:

- Question framework picker (PICO/PECO/PEO/PCC/SPIDER/SPICE) and why checking the restatement
  matters (feeds Stage 1's numbered criteria directly).
- Profile as overridable parts (scope/gates/max_articles), not just a bundle name.
- `pipeline.stop_after_stage: 5` as the pool-building move.
- Target setup commands (`library.py init --wiki` vs. `research.py init`) plus the
  `export wiki` / `pool.py migrate --from-wiki` conversion commands.
- Filters and outputs as the two "boring but decisive" remaining prompts.

## Zone of proximal development notes

This closed out "everything before the pipeline actually runs." [[scope-and-profile-shorthand]]
(Lesson 1) covered the top-level scope decision; this lesson covered the mechanics of answering
Stage 0 well. Confirmed ready to move into pipeline execution itself next (Lessons 3–5, filed as
[[full-pipeline-mechanics]]) rather than spending another lesson on configuration.
